const initState = {
  wishLists: [],
  error: null,
  loading: true,
};

const wishlistReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default wishlistReducer;
